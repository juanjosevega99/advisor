import { test, expect } from "@playwright/test";

const LOCALHOST_URL = "http://localhost:3000/quotes";

test("should render the cards with quote and author", async ({ page }) => {

});


test("should add a new quote when the button save is clicked", async ({ page }) => {

});
